public class Solution {
    public int[] TwoSum(int[] nums, int target) {
        int[] num = {0, 1};
        for(;num[0] < nums.Count(); num[0]++){
            int diff = target - nums[num[0]];
            for(num[1]=num[0]+1; num[1]<nums.Count(); num[1]++)if(nums[num[1]] == diff)
            
     
        return num;
        }
        return num;

    }
}
